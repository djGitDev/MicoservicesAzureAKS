apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pgdata1-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: default  # adapte selon ton cluster
