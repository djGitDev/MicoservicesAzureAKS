image: docker:24.0.7

services:
  - name: docker:dind
    alias: dind  # Alias court et simple

variables:
  DOCKER_HOST: tcp://dind:2375  # Utilisez directement l'alias
  DOCKER_TLS_CERTDIR: ""
  DOCKER_DRIVER: overlay2

stages:
  - build

before_script:
  - apk add --no-cache docker-cli-compose
  - sleep 15  # Pause simple pour laisser le service démarrer

build:
  stage: build
  script:
    - echo "🛠️ Construction des microservices"
    - docker compose version
    - docker info
    - docker compose build
    - echo "✅ Build terminé"